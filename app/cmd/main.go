package main

import "log/slog"

// main is a placeholder until the v2 application layer is wired up.
// The pre-v2 entrypoint lives in legacy_main.go behind the `legacy` build tag.
func main() {
	slog.Info("curtz: v2 wiring not yet implemented")
}
