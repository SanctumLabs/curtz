name: Lint

on: [ push ]

permissions:
  contents: read
  pull-requests: read

jobs:
  lint:
     name: Lint
     uses: SanctumLabs/ci-workflows/.github/workflows/go-lint.yml@main
     with:
       go-version: 1.25
